---
schema: agent-architecture/1
mechanisms: 日常の操作で止まらないようにする
generated-by: project-design-harness
updated: YYYY-MM-DD
source: docs/project-definition.md
---

# Agent Architecture: [プロジェクト名]

<!--
この文書の役割（このコメントは生成時に削除する）
- 正本は .claude/ 以下の実体。この文書は「なぜそうしたか」の短い根拠記録
- 用途は3つ。再実行のとき前回の判断を引き継ぐ／どの役を何に使うかを読めるようにする／部品を外すかどうかの判断材料にする
- 長く書かない。各項目は1〜3行。設定値の写しは書かず、ファイルを指す
-->

## 概要

- 入れた仕組み: （呼び名で書く。例:「日常の操作で止まらないようにする」まで）。選んだ理由: （ユーザーの言葉で1行）
- メンバー: 主セッション（設計の対話役）／（生成した subagent を列挙）
- 作業の流れ: 設計文書 → タスク契約 → テスト（`test-writer`）→ 実装（`implementer`）→ レビュー → 検証
- 入力にしたプロジェクト定義の更新日: YYYY-MM-DD

## 役の一覧

| 役 | 実体 | 何に使うか | 書ける場所 | 防ぐ失敗 |
|---|---|---|---|---|
| 主セッション | `CLAUDE.md` の役割定義 | ユーザーとの設計の対話、設計文書の更新、委任 | 設計文書の置き場 | 設計を経ずにコードが変わる |
| researcher | `.claude/agents/researcher.md` | ユーザーとの往復が要らない調査 | なし | 調査の生データで、設計の対話の文脈が埋まる |
| test-writer | `.claude/agents/test-writer.md` | テストコード。実装の前に、契約の「完了の証拠」をテストにする | テストの置き場、`docs/harness/**` | 通らないテストを書き換えて通す。実装の振る舞いをそのまま正としてテストにする |
| implementer | `.claude/agents/implementer.md` | 本体のコードと設定。先に書かれたテストを通す | テストと設計文書を除くすべて、`docs/harness/**` | 設計の対話と実装が同じ文脈に混ざり、設計が実装に引きずられる |
| reviewer | `.claude/agents/reviewer.md` | コードレビュー。設計文書との適合、読めること、直せること | なし | 検査には通るが、設計文書とずれたコード、後で直せないコードが通る |
| verifier | `.claude/agents/verifier.md` | 検証。契約に照らして、結果を壊しにいく | なし | 検査を走らせずに「終わった」と言う。作った本人の確認で済ませる |

### 条件つきで足した役、分けた役

<!-- 足しても分けてもいなければ、この表を消して「足していない。区分 D はすべて none で、ツールチェーンは1つのため」のように、理由を1行で書く -->

| 役 | 条件 | 防ぐ失敗 |
|---|---|---|
| security-reviewer | 区分 D に none でない項目がある | 秘密情報や個人情報の扱いの誤りが、機能の検証をすり抜ける |
| operator | 区分 D を根拠にした承認つきコマンドがある | 戻せない操作を、戻せる操作と同じ軽さで実行する |
| （implementer-〇〇、test-writer-〇〇） | ツールチェーンが複数ある | （具体的な失敗で書く） |

## Policy 表

ユーザーが確認した、操作ごとの扱い。実体は `.claude/harness.json` の `commands` と、`.claude/settings.json` の `permissions`。

<!-- 確認のときに利用者に見せた表を、そのまま写す。根拠の列には、もとにしたプロジェクト定義の行（区分と項目）を書く。どのプロジェクトにも入れる行は「共通」。仕組みで止められないものも、同じ表の行として載せ、規則の列に守り方を書く -->

| 操作 | 根拠 | 階級 | 扱い | 規則 |
|---|---|---|---|---|
| | D: | 外部作用 | 承認（主セッション・運用役） | hook `…` |
| | D: | 不可逆・機微 | 禁止 | hook `…` |
| 秘密情報ファイルを読む | 共通 | 機微 | 禁止 | permissions `Read(.env*)` ほか |
| ハーネス自身の変更 | 共通 | 機微 | 承認（subagent は禁止） | hook の保護パス |
| | D: | | **仕組みでは止められない** | （守り方） |

自動で許可する日常の操作: （「日常の操作で止まらないようにする」を入れた場合。`allow` に入れたコマンドの一覧。役ごとには分けられないので、すべての役に効く）

## 8区分の設計

<!-- 各区分は「このプロジェクトでどう実現しているか」を1〜3行で書く。入れなかった仕組みに当たる区分は「未導入（「〇〇」を足すと入る）」と、呼び名で書く。番号は書かない -->

### 1. Contract（タスク契約）

### 2. Context（地図と文脈）

### 3. Tools（道具の関門）

### 4. State（永続状態）

### 5. Policy（承認と禁止）

### 6. Verification（検証）

### 7. Recovery（回復）

### 8. Observability（観測）

## 部品一覧

このスキルが生成したものの一覧。ここに載っていないファイルは人が書いたものとして扱い、再実行のときも触らない。「防ぐ失敗」に答えられない部品は、外す候補である。

<!-- 「層」の列には、「土台」か、その部品が属する仕組みの呼び名（「依頼を契約にしてから始める」など）を書く。番号は書かない。生成したファイルは、この文書自身を含めてすべて載せる。ファイルの一部だけを生成した場合は、「CLAUDE.md（生成ブロック）」「.claude/settings.json（hooks と permissions の追記分）」のように、範囲を括弧で書く -->

| 部品（ファイル） | 層 | 防ぐ失敗 |
|---|---|---|
| `.claude/harness.json` | 土台 | 役ごとの書き込み範囲、設計文書とテストの置き場が、指示文の中にしか無い |
| `.claude/hooks/write-boundary.mjs` | 土台 | 設計書を経ずにコードが変わる。承認が要る操作を、承認なしに実行する。Agent が自分の権限を黙って広げる |
| `.claude/hooks/lib/harness.mjs` | 土台 | （上の hook の共通部品） |
| `.claude/agents/*.md`（役ごとに1行ずつ書く） | 土台 | （上の「役の一覧」の「防ぐ失敗」と同じ） |
| `.claude/settings.json`（Policy 下限） | 土台 | 承認が要るはずの操作を、実行できてしまう |
| `CLAUDE.md`（生成ブロック） | 土台 | 主セッションが、何を自分でやり何を委任するのかを知らない |
| `docs/agent-architecture.md` | 土台 | なぜこの構成にしたのかが残らず、再実行や部品の取り外しの判断ができない |

## 変更履歴

| 日付 | 変更 | 理由 |
|---|---|---|
| YYYY-MM-DD | 初回生成（入れた仕組みを、呼び名で） | |
