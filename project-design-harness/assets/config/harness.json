{
  "schema": "harness-config/1",
  "level": 3,
  "protected": [
    "**/.claude/**",
    "**/CLAUDE.md",
    "**/CLAUDE.local.md",
    ".mcp.json"
  ],
  "roles": {
    "main": {
      "write": [
        "-**",
        "+docs/**"
      ],
      "onDeny": "主セッションは設計の対話役です。書けるのは docs/ 以下の文書だけで、それ以外は委任します。{path} を変えるには、タスク契約を書いて（または更新して）implementer に委任してください。"
    },
    "implementer": {
      "write": [
        "+**",
        "-docs/**",
        "+docs/harness/**"
      ],
      "onDeny": "役 {role} は設計文書（{path}）を編集できません。変更は設計文書から始まります。設計が誤っている、または足りないと気づいたら、自分で直さず、何がなぜ問題かを報告して主セッションに差し戻してください。"
    },
    "researcher": {
      "write": [
        "-**"
      ],
      "onDeny": "役 {role} は読み取り専用です。分かったことは最後の報告で返してください。何を文書に残すかは主セッションが決めます。"
    },
    "*": {
      "write": [
        "-**"
      ],
      "onDeny": "Agent {role} には、このプロジェクトで書き込める役が割り当てられていません（{path}）。書けるのは .claude/harness.json に載っている役だけです。implementer に委任するか、役の追加をユーザーに相談してください。"
    }
  },
  "commands": [],
  "messages": {
    "protectedAsk": "ハーネスのファイル（{path}）を変更しようとしています。ハーネスの変更には、あなたの承認が必要です。",
    "protectedDeny": "subagent はハーネスのファイル（{path}）を変更できません。必要な変更は主セッションに報告してください。",
    "commandDeny": "この操作は、このプロジェクトでは禁止されています: {label}。別の方法で実現しようとせず、必要であることを報告してください。",
    "commandNotYourRole": "役 {role} は、この操作を実行しません: {label}。作業場所の外に影響が出る操作なので、担当は {roles} です。自分で実行せず、必要であることを報告してください。",
    "commandAsk": "作業場所の外に影響が出る、または取り消せない操作です: {label}。実行には、あなたの承認が必要です。",
    "verifyFail": "完了には証拠が必要です。終える前に「{command}」（実行場所: プロジェクトのルートから見て {cwd}）が通らなければなりません（{attempt} 回目 / 上限 {max} 回）。このコマンドがまだ無ければ、その場所で動くように用意してください。安い検査から順に走るようにします（構文 → 型 → 対象を絞ったテスト）。下の失敗を読み、関係のある何かを変えてから再試行してください。同じことを繰り返しても結果は変わりません。完了を主張するのではなく差し戻す場合（契約が曖昧、設計の誤り、要件の矛盾）は、最後のメッセージを「{marker}」で始めれば止まれます。",
    "verifyTimeout": "検証「{command}」が {sec} 秒で時間切れになりました。",
    "verifyGiveUp": "検証「{command}」は {agent} が {max} 回試みても通りませんでした。この作業は検証されていません。完了として扱わず、内容を確認してください。",
    "verifyNoDir": "検証コマンドを実行する場所「{cwd}」（プロジェクトのルートからの相対）が、まだ存在しません。"
  },
  "verify": null,
  "trace": {
    "enabled": false,
    "dir": "docs/harness/traces"
  }
}
